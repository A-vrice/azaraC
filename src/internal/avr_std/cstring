#pragma once
// azaraC - src/internal/avr_std/cstring
// Minimal <cstring> shim for the Arduino AVR toolchain (no libstdc++).
// avr-libc provides <string.h>.

#if defined(AZARAC_AVR_STUB)
#include <cstring>
#else
#include <string.h>
#endif
